set timeout=0
set default=0
insmod all_video
insmod gfxterm

# Let GRUB choose the best framebuffer mode exposed by firmware/QEMU.
# LionOS reads the exact mode reported by the Multiboot2 framebuffer tag.
set gfxmode=auto
set gfxpayload=keep
terminal_output gfxterm

menuentry "LionOS" {
    multiboot2 /boot/lionos.bin
    boot
}
